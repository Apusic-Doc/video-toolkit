# Feature 制作规则（RULE.md 模板）

> 本文件是 video-toolkit 项目通用的"feature 制作规则"模板。
> **新建一个视频项目时，把本文件复制一份到项目根目录（跟 `meta.json`、`resources/` 同级），
> 按项目实际品牌色/语音/文案要求改写"品牌与色调"一节，其余规则原则上保持不变。**
> 之后每个 feature 在录制/编写文案/合成前，都应先读一遍本文件，明确"要做什么、不能做什么"。

---

## 1. 一个 feature 必须产出什么

一个 `feature-NN-xxx/` 目录，最终应当包含：

| 文件 | 必须 | 说明 |
|---|---|---|
| `meta.json` | 按需 | 只写与项目级默认不同的字段（标题/副标题/配音偏移等），见第 4 节 |
| `recording.mov`（录屏模式）或 `slides/*.png`+`*.txt`（幻灯片模式） | ✅ | 素材来源，二选一 |
| `subtitles.srt` | ✅ | 解说词/字幕文本，配音与字幕烧录都以此为准 |
| `ai_dub.wav` | 由 `vt dub`/`vt redub` 生成 | AI 配音，生成后应先试听确认语速/停顿自然 |
| `${feature}.mp4` / `${feature}-no-cover.mp4` | 由 `vt mix`/`vt all` 生成 | 合成后的成片（含/不含封面） |
| `${feature}-sub.mp4` / `${feature}-no-cover-sub.mp4` | 由 `vt burn` 生成 | 硬烧字幕版本，**对外交付默认用这个** |
| `README.md` 或 `NOTES.md`（可选但推荐） | 建议 | 一句话说明这个 feature 演示的是什么功能、面向什么场景 |

不产出的东西（不要额外发明字段/文件）：不要在 feature 目录里放与本 feature 无关的素材、不要引入本规则未定义的 meta 字段——如确需新字段，先在项目级 `meta.json` 或 `docs/SPEC.md` 里定义清楚再用。

---

## 2. 内容与脚本规范

- **单个 feature 只讲一件事**：一个明确的功能点或一条业务流程，不要在一个 feature 里塞多个不相关的功能。
- **结构**：开场一句话点明"这是什么/解决什么问题" → 核心操作演示（真实点击/真实数据流转） → 结尾一句话总结价值。不需要寒暄式片头片尾，封面/封底已经承担了这个作用。
- **时长**：单个 feature 建议控制在 1–3 分钟内讲清楚；超过 3 分钟优先考虑拆成多个 feature，而不是压缩语速。
- **解说词语气**：客观陈述功能与操作步骤，避免营销式夸张用词；面向的是评审/客户方的技术或业务人员，不是消费者广告。
- **数据脱敏**：演示中出现的姓名、手机号、单位名称、金额、编号等一律使用示例/测试数据，不得使用任何真实客户或真实业务数据。

---

## 3. 录制/演示规范

- **窗口与分辨率**：录制窗口固定 1920×1080，不要在录制过程中调整窗口大小或切换分辨率。
- **鼠标与节奏**：移动到目标前有短暂停顿再点击，不要"瞬移"式跳转；点击后给界面反馈留出可见的停留时间，方便配音对齐。避免连续快速点击导致画面一闪而过看不清操作了什么。
- **无关内容不得贯穿全程可见**：如果界面上有会持续刷新/滚动的组件（行情、日志流、通知栏等）且与本 feature 演示内容无关，录制前必须关闭或用测试环境屏蔽，不能让它在整段视频里一直可见——哪怕内容本身不敏感，持续跳动的无关元素也会分散观感、且有意外露出真实数据的风险。
- **录制前防泄露检查清单**（人工过一遍，不能只凭记忆）：
  1. 屏幕上所有可见文字/数据是否都是示例数据，没有真实客户信息
  2. 通知栏、消息弹窗、浏览器书签栏、系统菜单栏（如日历/股票/邮件类小组件）是否已关闭或替换为空白
  3. 浏览器标签页标题、地址栏历史提示是否会露出敏感信息（建议录制前清空地址栏自动补全或使用无痕/干净 profile）
  4. 桌面壁纸、Dock、菜单栏时间以外的图标是否干净
  5. 如涉及登录，账号/密码是否为专用测试账号，不使用真实员工账号
- **返工标准**：录制完成后回放一遍，只要触发上述清单任意一条，都必须重录，不要"先凑合过、后期再说"——后期无法安全地遮挡已经烧录进画面的内容。

---

## 4. 品牌与色调（项目级，需按产品定制）

> 复制本文件到具体项目时，把下面的默认值替换成该产品的真实品牌色/字体/语音，并写进项目根目录的 `meta.json`，
> 让所有 feature 自动继承，feature 级 `meta.json` 原则上不覆盖这些字段（除非该 feature 有特殊需求）。

| meta 字段 | 用途 | 默认值示例 |
|---|---|---|
| `cover_accent_color` | 封面标题文字颜色，应取产品 UI 实际使用的主色（从真实界面取色，不要臆造） | `#222222` |
| `subtitle_style.font_name` | 字幕字体 | `PingFang SC` |
| `subtitle_style.font_size` / `margin_v` | 字幕字号/底边距，1920×1080 下的真实像素值 | `44` / `45` |
| `subtitle_style.color` / `outline` | 字幕文字色/描边色（ASS 格式 `&HAABBGGRR`） | 白字黑边 `&H00FFFFFF` / `&H00000000` |
| `voice` / `voice_en` | 中/英文配音音色，同一项目内所有 feature 保持一致 | 见 `vt ui` 语音选择器 |
| `logo` | 水印，未显式设置时自动从 `resources/logo.png` 探测 | — |
| `bgm` | 背景音乐，**必须显式**设为路径或 `true` 才生效，不会自动带出 | `false`（默认关闭） |

**色调一致性要求**：同一项目下所有 feature 的封面配色、字幕样式、配音音色必须一致，不允许每个 feature 各自随意选择——这正是项目级 `meta.json`（或 `vt ui` 的"项目设置"）存在的意义。feature 级 `meta.json` 只应覆盖标题/副标题/配音偏移（`dub_offset`）等内容性字段。

---

## 5. 限制 / 红线

- **不得包含真实客户信息**：项目名、客户名、真实姓名/手机号/单位名称等，不得出现在 feature 目录下任何会被提交到 git 的文件（脚本、meta.json、README、代码注释）中，也不得出现在录制画面里（见第 3 节清单）。
- **不得跨 feature 修改**：处理某个 feature 时，只读写这个 feature 自己的目录；不要顺带修改、甚至只读检查其他未被点名要处理的 feature 或其素材，除非用户明确要求。
- **不得偏离项目级配色/字体/语音**：非经明确说明，不在 feature 级 `meta.json` 里覆盖 `cover_accent_color`/`subtitle_style`/`voice` 等品牌类字段。
- **不得引入未经确认的新素材**：BGM、封底（outro）等会改变时长或增加音轨的元素，默认禁用，只有明确要求才在该 feature 的 `meta.json` 里显式开启。
- **不得在字幕/配音生成后手工微调时间戳而不同步**：如需要整体调整音画同步，统一通过 `dub_offset` 调整并重跑 `vt redub && vt burn`，不要手动编辑 `.srt` 时间戳或 `.wav` 文件本身。

---

## 6. 新建 feature 前的检查清单

1. 已确认这个 feature 要讲清楚的是哪一个具体功能点，且没有和已有 feature 重复
2. 已确认项目级 `meta.json`（品牌色/字体/语音）已经配置好，本 feature 不需要额外覆盖
3. 演示环境已按第 3 节清单清理干净，使用的是测试账号和示例数据
4. 文案/字幕草稿已过一遍第 2 节内容规范（单一主题、时长可控、无营销腔）
5. 录制完成后已回放检查，确认无敏感信息泄露、无冷场/瞬移式操作
